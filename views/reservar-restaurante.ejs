<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Reservar Restaurante</title>
    <style>
        body {
            background: #f5f5f5;
            font-family: 'Montserrat', Arial, sans-serif;
            margin: 0;
            padding: 0;
        }
        .reserva-container {
            max-width: 420px;
            margin: 3rem auto;
            background: #fff;
            border-radius: 16px;
            box-shadow: 0 2px 16px rgba(255,179,0,0.10);
            padding: 2.2rem 2rem 1.5rem 2rem;
            text-align: center;
        }
        .reserva-container h1 {
            color: #ffb300;
            font-size: 1.5rem;
            margin-bottom: 1.5rem;
        }
        .reserva-form label {
            display: block;
            margin: 1rem 0 0.3rem 0;
            color: #444;
            font-weight: 600;
            text-align: left;
        }
        .reserva-form input[type="date"],
        .reserva-form input[type="time"],
        .reserva-form input[type="number"],
        .reserva-form input[type="text"] {
            width: 100%;
            padding: 0.5rem;
            border-radius: 6px;
            border: 1px solid #ccc;
            font-size: 1rem;
            margin-bottom: 0.7rem;
        }
        .btn-confirmar, .btn-volver {
            display: inline-block;
            background: #ffb300;
            color: #fff;
            border: none;
            border-radius: 6px;
            padding: 0.6rem 1.5rem;
            font-size: 1rem;
            font-weight: 600;
            margin-top: 1rem;
            margin-right: 0.5rem;
            cursor: pointer;
            transition: background 0.2s;
            text-decoration: none;
        }
        .btn-confirmar:hover, .btn-volver:hover {
            background: #e6a100;
        }
        .btn-volver {
            background: #bbb;
            color: #222;
        }
        .btn-volver:hover {
            background: #888;
            color: #fff;
        }
    </style>
</head>
<body>
    <div class="form-container">
        <h2>Reservar en <%= restaurante.nombre %></h2>
        <form action="/restaurante/abonar" method="POST">
          <input type="hidden" name="restaurante_id" value="<%= restaurante.id %>">
      
          <label for="fecha">Fecha:</label>
          <input type="date" id="fecha" name="fecha" required>
      
          <label for="hora">Hora:</label>
          <input type="time" id="hora" name="hora" required>
      
          <label for="numero_personas">Número de personas:</label>
          <input type="number" id="numero_personas" name="numero_personas" min="1" required>
      
          <label for="notas">Notas:</label>
          <input type="text" id="notas" name="notas" placeholder="Opcional">
      
          <div style="margin: 20px 0;">
            <strong>Abono para separar la mesa:</strong>
            <span style="color: #FFA800; font-size: 1.2em;">$20.000 COP</span>
            <input type="hidden" name="monto_abono" value="20000">
          </div>
      
          <button type="submit" class="btn btn-warning">Confirmar y pagar abono</button>
          <a href="/restaurantes" class="btn btn-secondary">Volver</a>
        </form>
      </div>
</body>
</html> 