<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Reservar Hotel</title>
    <style>
        body {
            background: #f5f5f5;
            font-family: 'Montserrat', Arial, sans-serif;
            margin: 0;
            padding: 0;
        }
        .reserva-container {
            max-width: 420px;
            margin: 3rem auto;
            background: #fff;
            border-radius: 16px;
            box-shadow: 0 2px 16px rgba(255,179,0,0.10);
            padding: 2.2rem 2rem 1.5rem 2rem;
            text-align: center;
        }
        .reserva-container h1 {
            color: #ffb300;
            font-size: 1.5rem;
            margin-bottom: 1.5rem;
        }
        .reserva-form label {
            display: block;
            margin: 1rem 0 0.3rem 0;
            color: #444;
            font-weight: 600;
            text-align: left;
        }
        .reserva-form input[type="date"],
        .reserva-form input[type="number"] {
            width: 100%;
            padding: 0.5rem;
            border-radius: 6px;
            border: 1px solid #ccc;
            font-size: 1rem;
            margin-bottom: 0.7rem;
        }
        .btn-confirmar, .btn-volver {
            display: inline-block;
            background: #ffb300;
            color: #fff;
            border: none;
            border-radius: 6px;
            padding: 0.6rem 1.5rem;
            font-size: 1rem;
            font-weight: 600;
            margin-top: 1rem;
            margin-right: 0.5rem;
            cursor: pointer;
            transition: background 0.2s;
            text-decoration: none;
        }
        .btn-confirmar:hover, .btn-volver:hover {
            background: #e6a100;
        }
        .btn-volver {
            background: #bbb;
            color: #222;
        }
        .btn-volver:hover {
            background: #888;
            color: #fff;
        }
    </style>
</head>
<body>
    <div class="reserva-container">
        <h1>Reservar en <%= hotel.nombre %></h1>
        <form action="/hotel/reservar/<%= hotel.id %>" method="POST" class="reserva-form">
            <label>Fecha de entrada:
                <input type="date" name="fecha_entrada" required>
            </label>
            <label>Fecha de salida:
                <input type="date" name="fecha_salida" required>
            </label>
            <label>Número de personas:
                <input type="number" name="num_personas" min="1" max="10" required>
            </label>
            <button type="submit" class="btn-confirmar">Confirmar reserva</button>
            <a href="/" class="btn-volver">Volver</a>
        </form>
    </div>
</body>
</html> 