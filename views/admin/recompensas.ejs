<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gestionar Recompensas | EasyStay Admin</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/admin-dashboard.css">
    <style>
        .recompensas-container {
            background: #fff;
            border-radius: 14px;
            box-shadow: 0 2px 8px rgba(255,179,0,0.08);
            padding: 2rem;
            margin-top: 2rem;
        }
        .recompensas-container h2 {
            color: #ffb300;
            margin-bottom: 1.5rem;
            text-align: center;
        }
        .form-recompensa label {
            display: block;
            margin-bottom: 0.5rem;
            font-weight: 600;
            color: #555;
        }
        .form-recompensa input[type="text"],
        .form-recompensa input[type="number"],
        .form-recompensa input[type="date"],
        .form-recompensa textarea,
        .form-recompensa select {
            width: calc(100% - 20px);
            padding: 10px;
            margin-bottom: 1rem;
            border: 1px solid #ddd;
            border-radius: 8px;
            font-size: 1rem;
        }
        .form-recompensa button {
            background: #28a745;
            color: white;
            border: none;
            border-radius: 8px;
            padding: 12px 25px;
            font-size: 1.1rem;
            cursor: pointer;
            transition: background 0.2s;
        }
        .form-recompensa button:hover {
            background: #218838;
        }
        .recompensas-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 2rem;
        }
        .recompensas-table th, .recompensas-table td {
            padding: 0.8rem;
            border-bottom: 1px solid #eee;
            text-align: left;
            font-size: 0.95rem;
        }
        .recompensas-table th {
            background: #fffbe6;
            color: #ffb300;
            font-weight: 700;
        }
        .recompensas-table tbody tr:hover {
            background: #fff3cd;
        }
        .no-recompensas {
            text-align: center;
            color: #888;
            padding: 20px;
        }
        .message.success {
            background-color: #d4edda;
            color: #155724;
            padding: 10px;
            border-radius: 5px;
            margin-bottom: 1rem;
        }
        .message.error {
            background-color: #f8d7da;
            color: #721c24;
            padding: 10px;
            border-radius: 5px;
            margin-bottom: 1rem;
        }

        /* Sub-header styles */
        .sub-admin-header {
            background: linear-gradient(90deg, #ffb300, #ffc400);
            padding: 1rem 2rem;
            color: white;
            display: flex;
            justify-content: center;
            border-bottom-left-radius: 20px;
            border-bottom-right-radius: 20px;
        }

        .sub-header-content {
            display: flex;
            justify-content: space-between;
            align-items: center;
            width: 100%;
            max-width: 1200px;
        }

        .sub-header-left {
            display: flex;
            align-items: center;
            gap: 1.5rem;
        }

        .sub-header-title {
            font-size: 2rem;
            font-weight: 700;
            color: white;
            text-shadow: 1px 1px 2px rgba(0,0,0,0.1);
            margin: 0;
        }

        .sub-header-back-btn {
            background: #fff;
            color: #ffb300;
            padding: 0.6rem 1.2rem;
            border-radius: 20px;
            text-decoration: none;
            font-weight: 600;
            transition: all 0.2s ease-in-out;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
        }

        .sub-header-back-btn:hover {
            background: #f0f0f0;
            transform: translateY(-2px);
        }

        .sub-header-right {
            position: relative;
        }

        .admin-user-dropdown {
            position: relative;
        }

        .admin-user-btn {
            background: rgba(255, 255, 255, 0.2);
            border: 1px solid rgba(255, 255, 255, 0.5);
            color: white;
            padding: 0.6rem 1.2rem;
            border-radius: 20px;
            cursor: pointer;
            font-weight: 600;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            font-size: 1rem;
        }

        .admin-user-btn:hover {
            background: rgba(255, 255, 255, 0.3);
        }

        .admin-dropdown-content {
            display: none;
            position: absolute;
            right: 0;
            top: 100%;
            background: #fff;
            color: #333;
            min-width: 280px;
            box-shadow: 0 8px 16px rgba(0,0,0,0.2);
            border-radius: 10px;
            z-index: 101; /* Mayor que el z-index del header principal si existe */
            overflow: hidden;
            padding-top: 10px;
            padding-bottom: 10px;
            margin-top: 10px;
        }

        .admin-user-dropdown:hover .admin-dropdown-content {
            display: block;
        }

        .admin-user-info {
            padding: 12px 20px;
            font-weight: 600;
            border-bottom: 1px solid #eee;
            margin-bottom: 5px;
            background-color: #fdfdfd;
            color: #555;
            white-space: nowrap;
        }

        .admin-dropdown-content a {
            color: #333;
            padding: 12px 20px;
            text-decoration: none;
            display: flex;
            align-items: center;
            gap: 10px;
            font-size: 0.95rem;
            transition: background-color 0.2s;
        }

        .admin-dropdown-content a i {
            color: #ffb300;
            width: 20px;
            text-align: center;
        }

        .admin-dropdown-content a:hover {
            background-color: #f1f1f1;
        }

        .dropdown-divider {
            height: 1px;
            background-color: #eee;
            margin: 5px 0;
        }

        .actions-cell {
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .action-btn {
            background: none;
            border: none;
            cursor: pointer;
            font-size: 1.2rem;
            color: #007bff;
            padding: 5px;
            transition: color 0.2s;
        }

        .action-btn:hover {
            color: #0056b3;
        }

        .toggle-btn .fa-toggle-on {
            color: #28a745;
        }

        .toggle-btn .fa-toggle-off {
            color: #6c757d;
        }
    </style>
</head>
<body>
    <%- include('./partials/sub_header', { user: user }) %>
    <main class="admin-main">
        <h1>Gestionar Recompensas</h1>
        <div class="recompensas-container">
            <h2>Agregar Nueva Recompensa</h2>

            <% if (messages && messages.length > 0) { %>
                <div class="message success">
                    <%= messages %>
                </div>
            <% } %>
            <% if (error && error.length > 0) { %>
                <div class="message error">
                    <%= error %>
                </div>
            <% } %>

            <form action="/admin/recompensas/add" method="POST" class="form-recompensa">
                <label for="nombre">Nombre de la Recompensa:</label>
                <input type="text" id="nombre" name="nombre" required>

                <label for="descripcion">Descripción:</label>
                <textarea id="descripcion" name="descripcion" rows="3"></textarea>

                <label for="tipo">Tipo de Recompensa:</label>
                <select id="tipo" name="tipo" required>
                    <option value="">Seleccione un tipo</option>
                    <option value="descuento_hotel">Descuento en Hotel</option>
                    <option value="descuento_restaurante">Descuento en Restaurante</option>
                    <option value="comida_gratis">Comida Gratis</option>
                    <option value="upgrade_habitacion">Upgrade de Habitación</option>
                    <option value="beneficio_especial">Beneficio Especial</option>
                </select>

                <label for="puntos_requeridos">Puntos Requeridos:</label>
                <input type="number" id="puntos_requeridos" name="puntos_requeridos" min="1" required>

                <label for="valor_descuento">Valor de Descuento (solo para descuentos):</label>
                <input type="number" id="valor_descuento" name="valor_descuento" step="0.01" min="0">

                <label for="codigo">Código (opcional):</label>
                <input type="text" id="codigo" name="codigo">

                <label for="fecha_inicio">Fecha de Inicio (opcional):</label>
                <input type="date" id="fecha_inicio" name="fecha_inicio">

                <label for="fecha_fin">Fecha de Fin (opcional):</label>
                <input type="date" id="fecha_fin" name="fecha_fin">

                <label for="stock">Stock (0 para ilimitado, opcional):</label>
                <input type="number" id="stock" name="stock" min="0">

                <button type="submit"><i class="fas fa-plus-circle"></i> Agregar Recompensa</button>
            </form>

            <hr style="margin: 2.5rem 0; border: 0; border-top: 1px solid #eee;">

            <h2>Recompensas Existentes</h2>
            <% if (recompensas && recompensas.length > 0) { %>
                <table class="recompensas-table">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Nombre</th>
                            <th>Tipo</th>
                            <th>Puntos</th>
                            <th>Descuento</th>
                            <th>Código</th>
                            <th>Stock</th>
                            <th>Estado</th>
                            <th>Acciones</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% recompensas.forEach(r => { %>
                            <tr>
                                <td><%= r.id %></td>
                                <td><%= r.nombre %></td>
                                <td><%= r.tipo %></td>
                                <td><%= r.puntos_requeridos %></td>
                                <td><%= r.valor_descuento ? `${r.valor_descuento}%` : 'N/A' %></td>
                                <td><%= r.codigo || 'N/A' %></td>
                                <td><%= r.stock !== null ? r.stock : 'Ilimitado' %></td>
                                <td><%= r.estado %></td>
                                <td>
                                    <a href="/admin/recompensas/edit/<%= r.id %>" title="Editar"><i class="fas fa-edit"></i></a>
                                    <form action="/admin/recompensas/toggle-status/<%= r.id %>" method="POST" style="display: inline;">
                                        <button type="submit" class="action-btn toggle-btn" title="Cambiar Estado">
                                            <i class="fas <%= r.estado === 'activo' ? 'fa-toggle-on' : 'fa-toggle-off' %>"></i>
                                        </button>
                                    </form>
                                </td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
            <% } else { %>
                <p class="no-recompensas">No hay recompensas creadas aún.</p>
            <% } %>
        </div>
    </main>
    <%- include('../partials/footer') %>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script>
        const fechaInicioInput = document.getElementById('fecha_inicio');
        const fechaFinInput = document.getElementById('fecha_fin');
        const today = new Date().toISOString().split('T')[0];

        if (fechaInicioInput) {
            fechaInicioInput.setAttribute('min', today);

            fechaInicioInput.addEventListener('change', function() {
                if (fechaFinInput) {
                    fechaFinInput.setAttribute('min', this.value);
                    // Si la fecha de fin es anterior a la de inicio, ajustarla
                    if (new Date(this.value) > new Date(fechaFinInput.value)) {
                        fechaFinInput.value = this.value;
                    }
                }
            });
        }

        if (fechaFinInput) {
            // Asegurarse de que la fecha de fin no sea anterior a hoy al cargar
            if (fechaFinInput.value === '' || new Date(fechaFinInput.value) < new Date(today)) {
                fechaFinInput.setAttribute('min', today);
            }
        }

    </script>
</body>
</html> 